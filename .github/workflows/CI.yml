name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  # 1. Lint (qualité du code)
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Run ESLint
        run: npm run lint

  # 2. Tests unitaires hors conteneur
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Run Jest tests
        run: npm test

  # 3. Build & Push image (seulement si lint + tests passent)
  build:
    runs-on: ubuntu-latest
    needs: [lint, tests]   # dépend des deux jobs en parallèle
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ secrets.GHCR_USERNAME }} --password-stdin
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./fullstack
          push: true
          tags: |
            ghcr.io/${{ secrets.GHCR_USERNAME }}/fullstackprog-app:${{ github.sha }}
            ghcr.io/${{ secrets.GHCR_USERNAME }}/fullstackprog-app:latest

  # 4. Security Scan
  security:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Scan Docker image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ secrets.GHCR_USERNAME }}/fullstackprog-app:${{ github.sha }}
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true

  # 5. Mise à jour du repo infra (GitOps)
  update-infra:
    runs-on: ubuntu-latest
    needs: security
    steps:
      - name: Checkout infra repo
        uses: actions/checkout@v3
        with:
          repository: CL-KRMA/fullstackprog-gitops
          ref: main
          token: ${{ secrets.INFRA_REPO_TOKEN }}

      - name: Mettre à jour le manifest avec le nouveau tag
        run: |
          sed -i "s|ghcr.io.*/fullstackprog-app:.*|ghcr.io/${{ secrets.GHCR_USERNAME }}/fullstackprog-app:${{ github.sha }}|g" frontend.yaml

      - name: Commit et push
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add frontend.yaml
          git commit -m "Update fullstackprog-app image to ${{ github.sha }}" || echo "No changes to commit"
          git push origin main
